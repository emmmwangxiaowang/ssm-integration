<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http: //mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wang.pojo.Books">
    <insert id="addBook" parameterType="Books">
        insert into ssmbuild.books(boookID,bookName,bookCounts,detail)
        values(#{bookID},#{bookName},#{bookCounts},#{detail});
    </insert>

    <delete id="deleteByID" parameterType="int">
        delete from ssmbuild.books where id =#{bookID};
    </delete>

    <update id="updateBook" parameterType="Books">
        update ssmbuild.books set bookName=#{bookName},bookCounts=#{bookCounts},detail=#{detail}
        where id =#{id};
    </update>

    <select id="queryBookByID" parameterType="int">
        select * from ssmbuild.books where id =#{id};
    </select>

    <select id="selectAll">

                select * from ssmbuild.books;

    </select>
</mapper>